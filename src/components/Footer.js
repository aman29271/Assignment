import React from "react";

import bulma from "../scss/bulma.module.scss";
const Footer = () => {
  return false && <footer className={bulma["footer"]}>This is a Footer</footer>;
};
export default Footer;
